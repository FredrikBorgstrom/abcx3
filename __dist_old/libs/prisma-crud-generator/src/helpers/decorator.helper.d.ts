export declare class DecoratorHelper {
    name: string;
    importFrom: string;
    options: string;
    constructor(name: string, importFrom: string, options?: string);
    generateContent(): string;
}
